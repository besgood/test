pub fn catalog_persistence_tactics() -> Vec<String> {
    vec![
        "Cron job injection".to_string(),
        "User startup script backdoor".to_string(),
        "Systemd service replacement".to_string()
    ]
}
