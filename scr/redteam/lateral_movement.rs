pub fn simulate_lateral_movement() -> Vec<String> {
    vec![
        "Attempting pivot via SSH to internal network".to_string(),
        "Checking for WinRM or RDP endpoints".to_string(),
        "SCP file transfer emulation".to_string()
    ]
}
